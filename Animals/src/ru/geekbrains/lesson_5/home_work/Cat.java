package ru.geekbrains.lesson_5.home_work;

public class Cat extends Animal {
    public Cat() {
        super(200, 0, 2);
    }

    public Cat(int run_distance, int swim_distance, double jump_distance) {
        super(run_distance, swim_distance, jump_distance);

    }
}
